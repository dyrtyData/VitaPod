# VitaPod environment template. Copy to .env only if you need to override a value;
# .env is ignored by Git and must never hold real credentials or wallet material.
# The local demo (docker compose) needs no values here. Chain configuration for the
# contracts workspace lives in contracts/.env.example (added in the registry phase).
